# 产品规格：产品总览周度父ASIN权威来源切换

## 目标

将产品总览中的父ASIN周度经营指标切换为领星MCP直接返回的父ASIN周报，并以该来源替代现有的ASIN日快照周聚合任务。ASIN日数据继续存在，但其消费范围只限于单ASIN详情和库存规划；不得再为产品总览派生或覆盖父ASIN周度指标。

## 已确认的业务规则

| 主题 | 已确认规则 |
| --- | --- |
| 周度权威来源 | 领星MCP产品表现报表，`date_view_type=week`、`summary_field=parent_asin` |
| 店铺与站点范围 | 全部美国站已授权店铺 |
| 自然周 | 周一至周日；任务仅读取上一个完整自然周 |
| 首次只读预览 | 2026-08-24至2026-08-30 |
| 周度定时 | 每周一北京时间16:10 |
| 定时任务替换 | 停用旧`parent_asin_weekly_rollup`日快照聚合触发器；不得与新任务并行触发 |
| 写入规则 | 完整性校验通过后直接幂等追加；同一身份内容相同跳过，内容冲突转人工复核，绝不覆盖 |
| 历史保护 | 保留用户上传父ASIN周表、旧日聚合历史、同步批次、Trace和回滚基线 |
| 产品总览 | 仅读取已确认的MCP父ASIN周事实；同周仅在MCP事实缺失时回退显示上传周表并标注来源 |
| 单ASIN详情 | 展示ASIN日表现、日趋势和子ASIN明细；不将父ASIN周报拆分为日数据 |
| 库存规划 | 继续使用ASIN日表现和库存快照作为日销量/覆盖天数参考 |
| AI使用边界 | AI仅可消费状态为已确认的父ASIN周事实或ASIN日事实；不得跨粒度重算、推测或覆盖权威指标 |

## 成功标准

系统能在一个完整自然周后，通过唯一的受治理MCP周报任务生成可追溯父ASIN周事实；产品总览展示该事实并明确来源；ASIN详情和库存规划的日数据读取保持可用；缺页、缺店、无效Schema、身份缺失、数值异常或同身份冲突均不产生部分周事实写入。

## 非目标

本次不删除历史数据、不改动库存或广告关键词任务、不自动修改运营配置、不运行知识蒸馏或其他AI Skill，也不变更领星MCP的只读白名单与授权店铺范围。

---

# 已批准扩展：统一受控Amazon采集平台与智能图片建议双轨竞品分析

**批准日期：** 2026-09-13  
**详细设计：** `docs/specs/unified-competitor-acquisition-and-image-analysis-implementation-plan-2026-09-13.md`

## 扩展目标

建设一个替代现有内嵌Amazon HTML爬虫的统一受控采集平台，并以该平台为数据底座升级智能图片建议Step 0。系统必须支持主要竞品整套图片纵向分析、跨竞品同一卖点与同一表达方式图片批量点选、原有表达方向分析兼容，以及经人工确认后生成第一步综合结论。

## 扩展核心原则

| 原则 | 规则 |
|---|---|
| 受控采集 | Amazon公开商品数据只通过管理员启用的Provider采集，不使用非受控浏览器或旧内嵌爬虫自动降级。 |
| 执行审计 | 每次采集形成Job、Provider Run、Raw Artifact、Source Snapshot、Revision和Confirmed Snapshot。 |
| 人工决策 | 只有人工确认快照和分析Artifact可供AI及下游模块消费。 |
| 图片边界 | 竞品图片只作为内部研究证据，不得作为我方素材、生成参考图或自动发布。 |
| 双轨分析 | 主要竞品全图分析负责纵向理解；卖点表达方向负责横向比较；两者在综合结论中汇合。 |
| 迁移顺序 | 图片知识库先迁移，其他消费者分阶段迁移；全部完成后才禁用旧执行入口。 |

## 扩展功能范围

| 模块 | 必须交付 |
|---|---|
| Provider平台 | 能力矩阵、成本估算、预算、缓存、幂等、固定错误类别和多Provider路由。 |
| 采集治理 | Job/Run、原始Artifact、标准化、S3资产、Source Snapshot、Revision、Confirmed Snapshot和Consumer Link。 |
| 图片知识库 | 单ASIN、批量、链接和局部刷新改用确认快照；历史数据保持可读。 |
| 主要竞品全图 | ASIN卡片、完整图库、逐图事实卡、分区分析、整套策略、编辑确认和版本追溯。 |
| 表达方向联动 | 从所有确认竞品图库按卖点、表达方式、图位、ASIN和证明方式筛选并批量选择；原手工上传1–5张继续兼容。 |
| 综合结论 | 只合并已确认全图和表达方向Artifact，由人工选择后再供卖点梳理和图片大纲使用。 |
| 旧爬虫迁移 | 图片知识库优先；随后Listing知识库、产品知识库、项目竞品分析、转化率采集和监控。 |

## 扩展非目标

首期不自动定时刷新竞品数据，不承诺Amazon视频采集，不使用竞品图片作为生成素材，不自动发布Amazon内容，不自动确认AI结果，不批量重采历史数据，不删除既有知识库或监控历史。

## 已确认产品决策

| 决策 | 结论 |
|---|---|
| 采集形式 | 外部Provider API + 系统Provider Adapter + 皇帝Tool/Agent/Job/Run。 |
| Provider账号 | 公司管理员账号；先试用资格验证，通过后再付费。 |
| Step 0结构 | 主要竞品全图分析、卖点表达方式分析、综合结论三个页签。 |
| 主要竞品 | 每个项目一个主要竞品，可有多个对标和补充样本。 |
| 图库选图数量 | 不设5张硬上限；超过30张分批分析。历史手工上传继续1–5张。 |
| A+缺失 | 不阻塞主图分析，但必须显示字段状态；不得把未返回等同于没有。 |
| 旧爬虫 | 分阶段退役，Provider失败不自动回退旧爬虫。 |

## 扩展验收依据

以详细设计文档第19章的ACQ、IMG、SAFE和RET验收项，以及`DEV-PLAN.md`新增阶段的逐阶段验收标准为准。
