# 父ASIN每周草稿运行验收（2026-08-26）

用户明确授权手动运行一次已启用的`parent_asin_weekly_rollup`计划。运行复用任务`4rEeuGVc8r9WpCNJuD7bkV`的受治理服务，运行键为`weekly:2026-08-17`，范围为2026-08-17至2026-08-23。

| 验收项 | 结果 |
| --- | --- |
| 生成批次 | #30009，状态`ready_for_review` |
| 草稿范围 | 464个父ASIN＋店铺＋站点周度行，均未选中 |
| 日快照基线 | 读取3,801条已确认MCP日快照 |
| 日期覆盖 | 2026-08-17至23七日齐全，无缺失日、无全零异常、无上游追溯缺口 |
| 写入策略 | `draft_only`，`auto_apply=0` |
| 历史周表边界 | `lingxing_product_weekly`在本周窗口为0行；本次没有创建周度业务事实、没有覆盖人工财务数据 |
| 调度记录 | `last_status=succeeded`、`last_batch_id=30009`，保留计划任务追溯 |

> 本批次用于人工审阅父ASIN周汇总和异常摘要；系统未提供自动确认/应用入口，且不应将该草稿覆盖至历史周度表。

以相同任务UID和相同运行键再次调用服务，返回`skipped: idempotent`。只读复核显示该运行键仅保留1个周度草稿批次（#30009），`lingxing_product_weekly`在2026-08-17至23仍为0行，证明重试不会重复生成草稿或写入历史周表。
