# 领星同步页已登录会话检查（2026-08-26）

## 会话事实

用户已在托管预览登录为 `gaozhen shao`。在已登录会话访问 `/ops/lingxing-sync` 后，页面不再白屏，运营布局、同步预览表单、Phase 5 规则卡片、自动计划面板和批次历史表均成功挂载。

## 当前缺口

页面当前仍显示两个计划“未启用”，并显示“0 / 0 个批次”。这与此前已审计的每日/周计划及批次#3不一致，说明问题已从“前端组件未挂载”收敛为“当前服务端读取的计划/批次数据与既有审计数据未对齐”。后续需只读检查当前开发服务连接的数据库、工作空间解析和`lingxingSync.list`响应；不执行任何批次确认、业务数据写入或广告设置变更。

## 白屏结论

先前白屏来自开发服务重建后的Vite/esbuild进程失效；重启服务后页面已恢复。登录前的登录页不加载同步组件，不能用来判断历史批次查询是否运行。

## 历史批次与计划空状态修复

已登录页面中，后端以工作空间1读取批次#3和两条计划配置均返回成功；一次性Router诊断同样返回批次#3（已应用、3759行）与每日/每周计划（均启用）。根因是首次渲染时，店铺目录与广告Profile目录会调用官方MCP且共享QPS=1限制；它们与纯数据库的`lingxingSync.list`和`lingxingSync.listSchedules`被同一个tRPC批量请求合并，目录请求出现局部429、HTTP 207后，页面将历史与计划呈现为空。

修复后，页面等待两个目录请求初始状态结束，再独立发起历史/计划查询。这样目录读取出现限流时仍不会阻塞批次审计或自动计划状态。同步页面契约、每日计划与草稿运行定向回归共14项已通过；未调用确认、应用或广告写入入口。

## 已登录页面验收结果

修复后的真实会话页面已显示两个“运行中”的自动计划，且按钮为“暂停计划”；每日计划状态为`idle`、自动应用配置仍由后端治理，周计划同样为`idle`。历史区显示`2 / 2`个批次，其中批次#3为“已应用”、读取行数`3759`、范围`ALL_US · 2026-08-10 至 2026-08-16`，并显示“原始响应已归档”；批次#1保留为零行空结果。该页面验证不调用确认、应用、预算、竞价或广告设置修改入口。

## Phase 5官方MCP只读草稿验收

美国站的Listing主数据、广告搜索词和广告投放目标已通过皇帝Tool Gateway的只读白名单、QPS=1、Tool Run与Trace审计生成独立草稿。Listing批次`#30001`读取200行；搜索词批次`#30002`读取200行并过滤1个全范围聚合/空身份行；修复投放目标真实身份键为`target_id/key`后，批次`#30004`读取200行并过滤1个聚合行。三项批次均标记`previewOnly=true`、所有行`selected=0`且为`needs_review`，服务端确认入口显式拒绝，未写入Listing主数据、广告事实表或任何预算、竞价、否词、状态与结构配置。

页面已显示新增三个“只读”数据域和对应草稿批次。父ASIN原始流量仍不生成草稿：已验证的官方工具目录未提供一个独立、可确认且不会与子ASIN日快照重复累计的父ASIN原始流量源；在此证据缺口消除前，该域保持“数据未提供/不可执行”边界，不以子ASIN指标杜撰替代。

在自动化政策恢复后的已登录页面复验中，两个计划均显示“运行中”。每日卡片明确显示“校验通过自动追加日快照”，每周卡片显示“仅生成草稿”；同步历史仍显示批次#3及#30001/#30002/#30004。页面不提供Phase 5批次的确认或应用入口。首次挂载时目录与历史查询会异步加载，短暂的`0 / 0`占位不代表数据缺失；目录读取结束后恢复为`6 / 6`批次及两项运行中计划。
