# 青岛ECS已部署优化恢复审计

**审计日期：** 2026-08-25  
**范围：** 恢复青岛ECS已部署、但此前未保存到本地检查点的源码与前向迁移；不读取或提交环境文件、密钥、构建产物和业务数据。

## 结论

青岛ECS源码明显领先原本的本地设计检查点 `62488a15`。已将应用源码受控回填至本地工作区，并完成对皇帝对话任务管理器、Harness/Run Ledger、上下文来源溯源、领星MCP治理与相关前端页面的恢复验证。

| 分类 | 恢复或核验结果 | 数据安全边界 |
| --- | --- | --- |
| 对话任务管理器 | 恢复三栏会话页、受治理对话Router、附件Artifact登记、知识引用、可编辑计划、审批、Skill/Agent/Tool委派与恢复逻辑 | 附件只存对象存储/Artifact引用；不开放任意Shell或未登记工具 |
| 执行恢复与可追溯性 | 恢复并补齐0163–0168：消息与Skill Run关联、状态版本、快照、幂等恢复请求与上下文来源溯源 | 全为新增字段/表/索引；未删除或覆盖历史记录 |
| Harness治理 | 恢复人审、反馈、质量门、受控并行、Run Ledger等能力；修复Preset未传递至Skill执行链路 | Preset只影响已登记模型Provider的候选顺序，不携带端点或密钥，也不绕过Skill/Tool治理 |
| 领星接入 | 恢复官方MCP只读白名单、店铺范围、QPS=1与Trace实现 | 不修改广告预算、竞价、投放状态或历史业务数据 |

## 数据库迁移核验

托管数据库原Schema已包含0158–0162，但历史账本在0137存在“重复列”失败记录。经只读核验现有表、Skill与OSS provider enum 后，已将**既有Schema基线受控登记至0162**；账本状态为 `baselined`。随后使用项目迁移脚本成功应用0163–0168；账本状态均为 `succeeded`。关键恢复字段以及 `emperor_execution_state_snapshots`、`emperor_execution_recovery_requests`、`emperor_context_source_provenance` 三张表均已存在。

> 基线操作只修复迁移账本元数据；0163–0168仅追加字段、表和索引。没有执行删除SQL、业务数据回填或覆盖操作。

## 质量验证

以下定向Vitest回归已通过：对话策略与上下文、计划恢复、对话前台契约、Harness完整性、Skill JSON容错与Preset归一化、Agent上下文、工件单一事实源、AI Job恢复、迁移源一致性及领星治理边界。针对本轮修改文件的ESLint检查也已通过。

完整 `pnpm check` 在当前沙箱被系统以 `SIGTERM` 终止，未产生TypeScript诊断；该资源限制不应被解释为编译成功。开发服务重启后的语言服务状态为无类型错误，保存后仍应在青岛ECS或资源充足环境执行一次完整编译。

## 待处理项

生产规划Skill仍可能返回“AI服务暂时不可用”。该问题与本次源码恢复、迁移补齐相互独立；后续应通过皇帝Skill Runner和模型网关的脱敏状态/HTTP分类诊断，不得建立绕过皇帝Skill体系的独立模型调用。

青岛ECS本次远程只读复核会话未返回，故未对生产实例重复执行迁移或重启服务。其已部署源码此前已完成只读导出；本地恢复与托管迁移已保存为独立可回滚版本。
