# 产品详情与产品总览数据完整性调查记录

## 2026-09-07 应用后服务与总览日志只读核验

- 已通过青岛 Workbench 以只读命令核验：`amazon-listing-web.service`、`amazon-listing-worker.service`、`amazon-listing-scheduler.service` 均为 `active`，本机 `http://127.0.0.1:3000/` 返回 HTTP `200`。
- 最近30分钟 Web 服务日志未检出 `getProductOverviewWithWeeks`、`parentWeekly`、tRPC 异常或未处理异常摘要。该结果仅说明服务和路由没有记录到运行错误，**不等同于产品总览已经完成数据呈现验收**。
- 本次没有启动领星MCP、没有重跑历史应用器、没有迁移或业务写入。23个完整自然周仍以此前已完成的只读覆盖审计为准。
- 浏览器验收中产品总览暂显示0个产品。下一步将只读核对权威父周事实实际写入的 `sourceKind`、最近周集合及总览过程筛选契约，以排除来源标签或查询返回空集不一致。
- 已提交一次性只读“总览可见性”审计器：脚本已通过本地语法和无写入关键词检查，并使用 SHA-256 `70510f5def532ecc99ffb33038af6af671bf4f3113f21af1e1b19983f0b68809` 在实例侧复核后，以受管运行环境执行。输出仅限来源标签、工作空间、周集合和聚合计数；不输出店铺、ASIN、金额、凭据或业务明细。
- 审计结果：权威来源标签为 `lingxing_mcp_parent_asin_weekly`，工作空间为1，共7,372条父周事实、24个自然周、337个父级业务身份；最近五个周窗口共有1,677条事实，仍对应337个父级业务身份。该结果与 `getProductOverviewWithWeeks` 的来源与窗口条件一致，**数据库事实不为空、来源标签也未错配**。
- 前端直达 `/ops/products` 时初始画面仍处于加载状态；下一步只读等待客户端请求完成，并检查页面/控制台中的查询结果，区分异步加载延迟、认证会话、前端运行错误与接口返回空集。
- 浏览器直连页面两次均先呈现根节点及加载指示器，随后自动化会话转为 `about:blank`，未能保留客户端控制台或请求记录。这是当前自动化浏览器的验收限制，不能作为“接口返回0条”的证据；服务日志也未记录总览过程错误。后续以受控页面诊断或用户侧截图补足最终视觉验收。
- 在加载阶段的浏览器控制台未出现前端异常；但随后的同源只读tRPC探针因页面上下文已转为无有效基址而未能构造URL，未发送有效请求。该失败没有触发数据读取、MCP调用或任何写入，也不能被解释为应用接口失败。
- 为避免任意传输、认证或服务端查询失败被误解释为“0个产品”，产品总览已新增父ASIN周度查询错误态：明确说明失败、显示脱敏错误摘要并提供“重试加载”入口；正常空态仍仅在请求成功且筛选后无数据时显示。相关前端契约测试、涉及文件ESLint和生产构建均已通过。
- 后续认证页面复验已完成：初始加载完成后，产品总览显示436张在售卡片，且“最近4周”表格已渲染父级周度行；这证明此前“0个产品”只是异步加载期间的临时状态，不是父周事实缺失、接口空集或前端崩溃。页面同时确认来源卡片已可见，但仍使用“`MCP 周报`”及“领星MCP父ASIN自然周报”等不准确表述。
- 根因是前端展示字符串残留了已废弃的“领星MCP直接提供父ASIN周报”能力假设，数据计算、来源筛选和23周事实写入均未受影响。已在本地修正产品总览卡片、说明条、页面来源说明、统一产品详情标签和领星同步任务说明，统一为“领星ASIN周数据·系统父ASIN汇总”；同时新增契约断言，阻止旧表述回归。最终定向Vitest、涉及文件ESLint与生产构建通过；全量TypeScript仍有169项既有诊断，但本轮变更文件无新增诊断。该命名修复尚待受控发布到青岛，并需重新加载页面确认。
- 青岛受控发布已完成：远端脚本和运行包SHA-256均通过，`RELEASE_OK release=parent-week-label-43dc4ea0`，且保留了运行目录备份。脚本仅原子替换`dist`并重启Web、Worker、Scheduler后完成本机HTTP健康检查；未运行迁移、领星读取、业务写入或调度变更。自动化浏览器随后导航`/ops/products`超时并回到空白上下文，故新的来源文字仍待一次认证页面重载确认，不能据此误报为页面失败。

## 截图证据

用户提供的子ASIN详情截图为1508×336像素的超宽表格，已按从左至右、含重叠区域的三块图像检查。前两块显示“子ASIN变体销量（4）”模块在“近4周”筛选下列出4个子ASIN，SKU均显示为“-”。“最近4周销量”和“平均日销”两列在四行中均为空白，而非明确的零值。

该现象证明详情页面当前未将所需日粒度销售指标映射至该表格，或查询结果被来源/身份过滤排除。它尚不能单独证明数据库无日数据，因此后续将结合页面请求、服务端查询和生产库聚合指标确认根因。

第三块截图显示同一模块的“可售库存”和“在途库存”两列存在数值，且与左侧四个子ASIN行逐行对应。由此可验证当前详情组件已取得库存来源数据，但未同时呈现销量、利润或广告日数据；问题更可能位于详情聚合字段覆盖或前端列合同，而不是整个详情查询没有返回记录。

第二张用户截图显示产品总览的父ASIN卡片在最近四个自然周中同时展示销售、订单、销售额、订单利润、Session、广告点击/曝光/花费等周度指标，并在卡片顶部显示子ASIN数量。该截图未能独立确认哪一条父ASIN记录重复，但可确认周度卡片的指标来源和“子ASIN数量”均由产品总览主查询提供；后续需围绕该查询的来源优先级、身份去重和成员计数进行复现。

## 已确认的代码链路证据

### 导入详情页（截图对应的“子ASIN变体销量”表）

1. `OpsProductDetail.tsx` 在导入模式调用 `dataImport.getLingxingDailyVariants`，表格读取 `salesQty`、`avgDailySales`、`fbaAvailable` 和 `fbaInTransit` 四类字段。现有表头没有利润、广告、销售额、Session 或广告归因列，因此该表即便上游存在这些事实，也不会向用户展示。
2. `getLingxingDailyVariants` 以 `userId + parentAsin` 查询日快照，并将非库存专用快照传给 `summarizeVariantSales`。该查询没有以店铺或站点进一步收窄父ASIN成员。
3. `summarizeVariantSales` 在来源选择时使用 `storeName|country|asin|reportDate`，但在最终变体分组时仅以裸 `asin` 作为键；同名子ASIN如存在于不同店铺或站点，会被合并为一行。该函数只返回嵌套的 `weekly` 销量数组和最新库存字段，未返回页面实际读取的顶层 `salesQty`、`avgDailySales`，故截图中销量两列为空具有直接代码证据。
4. 日快照模式已经具备销售、订单、销售额、订单利润、广告花费、广告销售额、广告订单、Session、广告点击/曝光及库存字段；缺失是详情聚合/展示合同不完整，不能按无数据处理，也不得在无来源时伪造为零。

### 系统详情与系统总览

1. `product_profiles` 与 `product_variants` 是独立的手工产品档案表。变体表只以 `product_id + child_asin` 保存成员，没有店铺或站点字段，也没有唯一约束。系统详情的 `getProduct`、产品列表的子ASIN数量以及利润、库存、广告摘要均从该手工变体表开始构造成员集合。
2. `getProductOverviewWithWeeks` 逐条返回 `product_profiles` 记录，以 `product_id` 查询 `product_weekly_ops`，并以 `variants.length` 填充子ASIN数量；前端只忠实渲染后端返回记录，不会执行后端级去重。因此若相同父ASIN在同一业务身份下存在重复档案，或手工变体与领星日快照成员集合不同，就会表现为总览重复、子体数不准及详情摘要漏匹配。
3. 已应用的领星MCP父ASIN周报事实写入 `lingxing_product_weekly` 时使用 `storeName|country|parentAsin|weekStartDate|weekEndDate` 的身份校验，且同来源冲突会失败关闭、不会覆盖历史事实。该层已有正确的周度身份边界；问题高风险点是总览查询仍按手工产品档案读取而非直接消费该规范身份。

## 受限生产核验与待验证假设

当前恢复后的会话未加载青岛SSH身份，阿里云控制台也未登录；因此尚未执行任何生产库读写或业务MCP调用。以下假设仅基于本地代码和用户截图，待获得可用生产只读访问后以聚合、脱敏计数验证：

| 假设 | 支持证据 | 最小验证方式 | 当前状态 |
| --- | --- | --- | --- |
| H1：手工变体成员表不完整、过时或重复，导致系统详情成员数与三类摘要过滤错误 | 系统详情、列表、利润、库存及广告摘要均依赖 `product_variants`，且该表不含店铺/站点身份 | 比较同一工作空间中手工成员与日快照规范成员集合的交并差计数，并统计 `product_id + child_asin` 重复数 | 待生产聚合核验 |
| H2：导入详情按裸ASIN聚合跨店/站点数据，且返回合同漏掉顶层销量字段及利润/广告字段 | `summarizeVariantSales` 的最终Map键为 `asin`，返回值只有 `weekly` 和库存字段 | 统计同一 `asin` 跨店/站点出现数量；用固定样本断言返回一行一个 `storeName|country|parentAsin|asin` 身份且字段合同完整 | 前端/后端合同缺失已确认；跨身份影响待生产核验 |
| H3：系统总览按手工产品档案逐条返回，未以父ASIN周报权威身份消除重复 | `getProductOverviewWithWeeks` 不读取 `lingxing_product_weekly`，前端无去重，周报事实层已有完整身份键 | 统计同工作空间、同 `storeName|marketplace|parentAsin` 的产品档案重复数，并与MCP周事实身份计数比对 | 待生产聚合核验 |

在H1-H3完成前，不应批量回填、删除或覆盖历史产品档案、变体或周度事实。修复应优先在查询层建立可复用的规范成员集合，并让详情展示对缺失来源明确显示“未提供”。

## 已实施的最小修复

| 问题 | 已实施处理 | 数据边界 |
| --- | --- | --- |
| 子ASIN详情销量为空、指标不足 | `summarizeVariantSales` 现按 `parentAsin|storeName|country|asin` 归并，并返回近选定周数的销量、平均日销、销售额、订单利润、广告花费/销售额、ACoS、Session、可售/在途库存和最新数据日期。导入详情与系统详情均复用该合同。 | 日均销量只在所有预期日期均有日快照时计算；不足覆盖显示“未提供”，而不是补零。广告和利润只取同一日快照事实，不与父ASIN周报或手工摘要重复累计。 |
| 导入模式跨店铺混合 | ERP总览改为按 `parentAsin|storeName|country` 分组；从卡片进入详情时，店铺和站点作为只读范围参数向后端传递。 | 同父ASIN在不同店铺或站点保持独立业务身份，不会合并指标或成员数。 |
| MCP总览重复、父子数量失真 | 系统“MCP父ASIN周报”页签改为直接读取当前窗口内的 `lingxing_mcp_parent_asin_weekly` 事实，通过唯一身份键去重；手工产品档案仅提供可选的管理元数据。子ASIN数量优先来自权威周报中的明确成员字段，只有来源未提供成员时才回退到该档案下的精确手工变体记录。 | 不删除、回填或覆盖手工档案、变体、历史周表或周度事实。无匹配手工档案的来源卡片可浏览周数据，但负责人分配、同步、详情和删除操作均禁用，防止误写。 |

## 本地验证结果

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| 日快照聚合单元测试 | 通过，7项 | 覆盖指标合同、跨店铺同ASIN隔离、完整周日均销量以及自然周边界。 |
| 父ASIN周报总览单元测试 | 通过，3项 | 覆盖MCP来源优先、跨店铺身份独立、重复手工档案仅生成一张卡片、来源成员计数。 |
| 相关总览与产品路由回归 | 通过，35项 | 覆盖来源优先、详情/总览契约和既有产品路由。 |
| 定向ESLint与完整生产构建 | 通过 | 涉及的前端、路由和聚合文件无新增规范错误；前后端构建成功。 |
| 完整TypeScript检查 | 受既有169项诊断阻断 | 日志未显示本轮变更文件新增诊断；现存首要错误仍位于与本轮无关的 `skillDistillation.ts` 空值类型。 |
| 本地页面截图 | 构建与路由可加载，但无法完成真实数据渲染验收 | 本地开发数据库连接已处于历史性关闭状态，页面请求停留在加载/空态；未将该环境错误归因为本轮改动。 |

青岛生产库尚未进行本轮核验，也未执行任何青岛代码部署、数据库写入或领星MCP调用。需要在具备受控访问后，按H1-H3中规定的脱敏聚合查询验证真实成员差异、总览返回行数和详情字段覆盖，再确认独立站部署结果。

## 青岛访问会话状态（2026-09-05）

用户已完成阿里云控制台登录。控制台可见唯一目标ECS实例处于运行和健康状态；本轮没有执行重启、停止、续费、密码重置、部署、数据库写入或领星MCP调用。浏览器自动化已打开“远程连接”对话框，但Workbench网页终端未能在当前自动化浏览器中建立可交互Shell；另行尝试的非官方直达域名因DNS不存在而失败，未重复尝试。

因此，生产数据库的脱敏只读聚合审计和独立站部署仍未开始。后续应从控制台“远程连接 → Workbench”真实终端，或恢复受控SSH身份后执行；必须先运行只读结构和计数查询，待结果确认后再进行备份与部署。

### 终端只读基线补充

已通过Workbench免密会话进入实例，并验证终端输入通道可用。对`/opt/amazon-listing-tool`执行的只读目录和容器检查显示：项目根目录未发现`docker-compose`或`compose`编排文件，`docker ps`仅返回表头且没有运行容器。因此，历史运行手册中的Docker容器假设不适用于当前实例状态；后续审计将先通过`systemctl`和进程清单定位实际Web、Worker、Scheduler与MySQL的运行方式，再使用对应的只读连接执行数据库结构和脱敏聚合查询。本轮尚未读取环境文件、凭据或业务明细。

生产MySQL结构查询已确认目标表均存在。`product_profiles`和`product_variants`延续既有camelCase业务列（包括`parentAsin`、`productId`、`childAsin`、`workspaceId`），而`ops_asin_daily_snapshots`使用snake_case来源事实列（包括`parent_asin`、`store_name`、`country`、`report_date`、销量/利润/广告/库存相关字段和来源标识）。该差异正是此前远程审计不能按源码或单一命名风格假定字段的原因；后续所有生产SQL将依据该结果使用明确的实际列名，且仅返回聚合计数。

父ASIN周事实实际表为`lingxing_product_weekly`。其规范列为`week_start_date`、`week_end_date`、`parent_asin`、`store_name`、`country`、`source_kind`、`source_batch_id`、`source_trace_id`和`source_schema_version`；同时存在历史兼容的camelCase列。首轮跨表脱敏聚合SQL因遗漏显式指定数据库而返回`ERROR 1046 (3D000): No database selected`，未读取任何行、未修改任何数据。后续将以`amz_listing`为显式数据库重试同一只读计数审计。

显式选择数据库后的第二轮聚合，已安全返回两项关键计数：手工`product_profiles`共有500行，但按`parentAsin|storeName|marketplace`归并只有3个身份组；`product_variants`中存在20个重复的`workspaceId|product_id|child_asin`身份组，共涉及45行。该结果直接支持“手工档案/变体不能作为MCP总览唯一来源或成员数唯一来源”的修复决策。该轮在读取日快照时因生产表实际工作空间列为`workspaceId`而非查询中误写的`workspace_id`中止，报`ERROR 1054 (42S22): Unknown column 'workspace_id'`；未读取业务明细、未运行后续查询、未产生任何写入。后续将只替换为已确认的`workspaceId`后复跑。

### 生产脱敏聚合核验结果

第三轮仅聚合、仅计数的审计已完成，未读出任意店铺、ASIN、金额、广告词或凭据。结果如下。

| 核验项 | 结果 | 结论 |
| --- | ---: | --- |
| 有效ASIN日快照 | 43,387行；1,022个`store|country|parent|child`规范子体身份；最新日期为2026-09-05 | 生产中有足够的日级指标事实，详情为空不是“无可用日数据”的普遍问题。 |
| 日快照非零覆盖 | 销量11,685行；利润30,140行；广告13,925行；库存28,990行 | 销售、利润、广告、库存均在日快照事实中存在；页面此前未展示是合同与聚合路径缺陷。零值仍保留为来源事实，不被解释为缺失。 |
| 日快照成员未命中手工变体 | 1,022个规范子体身份 | 手工`product_profiles/product_variants`不能代表生产日事实成员集合；以它们计算详情成员或摘要会系统性漏匹配。 |
| 同一裸ASIN跨店铺/站点出现 | 406个ASIN身份组 | 已验证`asin`裸键会产生跨店/站点冲突，必须使用`parentAsin|storeName|country|asin`规范键。 |
| MCP父ASIN周报事实 | 337行，对应337个`store|country|parent|natural_week`身份 | MCP周事实层本身同一身份唯一；系统总览重复不来自该权威事实表的同键重复。 |
| 上传父ASIN周表 | 6,875行，对应6,875个来源身份 | 历史上传表保留为参考来源，不纳入默认MCP总览，以避免跨来源混合。 |
| MCP周事实同键重复组 | 0组 | 当前周度权威事实可直接作为总览去重基准。 |

综合代码审计与生产聚合证据，根因结论为：**H1已确认**，过时且有重复的手工产品/变体表被用作成员及下游过滤基础，造成子体数和详情摘要失真；**H2已确认**，按裸ASIN聚合会混合406个跨店/站点身份；**H3已排除为事实表同键重复**，MCP周事实唯一，总览重复由手工档案投影/旧路径造成。已保存的`e93d3efa`通过规范身份键、日指标合同及权威周事实直读处理这些根因；尚未部署到青岛。

### 青岛部署基线（只读）

运行实例上的`/opt/amazon-listing-tool`包含编译后的应用目录，但执行`git status`/`git log`返回“不是Git仓库”。因此该目录不是可安全执行`git pull`的工作树，不能以本地项目或GitHub版本号直接推断其运行版本，也不得在未定位发布入口前覆盖目录。进程清单已确认该目录下存在Node编译产物进程；后续将以只读方式识别对应的systemd单元或其他受控启动入口，再按“备份当前编译目录与服务状态 → 部署单一已保存版本 → 健康检查 → 受限页面验收”的路径执行，且部署前须由用户明确确认。

用户确认部署后，已创建当前`dist`、`package.json`和锁文件的时间戳备份归档，并记录SHA-256校验值；`amazon-listing-web.service`、`amazon-listing-worker.service`和`amazon-listing-scheduler.service`在备份时均为`active`。已从仅含`dist`和`package.json`、不含环境文件或数据库内容的版本`c4dcce75`工件下载到新暂存目录；远端对工件SHA-256校验返回`OK`，且暂存目录内`dist/index.js`和`package.json`均存在。Workbench会话在发布前只读检查完成后发生瞬时断开；尚未替换`/opt/amazon-listing-tool/dist`、未重启服务、未运行迁移或数据写入。重新建立会话后将先核对暂存目录仍在，再进行一次原子替换与受管服务重启。

发布准备阶段出现了两次Workbench远程会话断开，均发生在短命令完成之后；实例状态始终显示运行中，重新使用免密连接可建立新会话。该现象目前仅记录为远程终端连接不稳定，尚无证据将其归因为应用服务、数据库或本轮代码。当前已重新发起会话，待终端连接完成后继续，不会重复创建备份、工件或暂存目录。

原子发布命令提交后，Workbench连接再次中断，终端来不及回显服务状态。当前已恢复新会话，下一步只执行目录切换状态和服务状态的只读判断：若旧`dist`目录已保留且暂存的`dist`已被移入运行目录，则进入健康核验；若未切换或状态不一致，则基于已归档备份恢复到一致状态后再报告。不会在未确认状态前重复执行替换命令。

发布后状态检查确认：旧运行目录`dist.pre-c4dcce75`存在，暂存目录中的`dist`已不存在，新的运行目录`dist/index.js`存在，判定为`deployment_state=swapped`。`amazon-listing-web.service`、`amazon-listing-worker.service`和`amazon-listing-scheduler.service`均返回`active`。因此原子替换与重启已成功完成；未执行数据库迁移、写入或MCP调用。下一步将执行本地HTTP健康检查、服务错误日志检查及受认证约束的页面入口检查；如任一健康检查失败，将使用保留的旧目录和归档备份立即回滚。

部署后健康检查通过：运行中的`dist/index.js`SHA-256与版本`c4dcce75`本地构建入口完全一致；`http://127.0.0.1:3000/`和`/ops/products`均返回HTTP 200；Web、Worker和Scheduler均为`active`。三项服务在近10分钟日志中`uncaught`、`fatal`、`TypeError`或`ReferenceError`的匹配计数均为0。由此确认部署后的服务基础健康，不触发回滚；仍需在已认证浏览器中对产品总览、单ASIN详情及指标表执行功能级核验。

对`https://kuahaixing.com/ops/products`的沙箱浏览器导航在90秒内超时，随后浏览器停留于空白页，未获得可用的外部页面响应或认证会话；这不足以判定生产前端异常。青岛本机`/ops/products`的HTTP 200和服务日志无新增致命错误仍是当前有效健康证据。下一步将从非认证网络路径核验域名响应，再依据结果决定是否需要由已登录用户浏览器完成最终UI回归。

非认证网络路径随后验证`https://kuahaixing.com/`和`/ops/products`均从青岛公网地址返回HTTP 200。将沙箱浏览器改由HTTP入口导航后可加载独立站的HTML标题，但视口仍为空白，且浏览器控制台无输出；当前会话未拥有业务系统认证信息，不能据此断言真实已登录页面的渲染结果。后续以受认证的真实页面验收为准；在此之前，服务器入口校验、HTTP 200、服务活动状态和日志零致命错误是已确认的发布健康证据。

浏览器DOM与资源摘要进一步确认：页面已完整加载，`#root`存在并挂载2个子节点，登录页显示“AMZ 全链路”和“登录使用”。版本对应的JavaScript与CSS资源均返回HTTP 200；`auth.me`相关批量请求在未认证会话中返回部分207/后续401，符合未登录状态，并非本轮产品接口故障。需由具备业务系统登录态的用户进入`/ops/products`后，才能对真实产品卡片、详情页和子ASIN指标进行交互级验收。

用户完成青岛独立站登录后，浏览器已能在根路径显示已认证首页和当前用户欢迎信息。随后直接进入`/ops/products`，页面已显示加载指示器、未出现错误边界或控制台异常；等待数据查询完成后继续读取卡片数量、周报来源状态及详情入口。

已认证页面的`productOps.getProductOverviewWithWeeks`批处理请求返回HTTP 200，产品总览显示“领星MCP父ASIN自然周报”为权威来源，共263个在售卡片。但该响应的263行全部`weeks=[]`，页面因此均显示“暂无周度数据”；其首行字段同时缺少本轮权威构建器必有的`isManaged`和`inventory`字段。生产MCP父周事实已只读确认有337个唯一身份，故这不是事实表缺失，而是线上请求未实际走到预期的`buildParentWeeklyOverview`返回合同。虽然`/opt/amazon-listing-tool/dist/index.js`的文件哈希与本地工件一致，仍需只读核对`amazon-listing-web.service`的`ExecStart`、`WorkingDirectory`及实际监听进程，以排除服务启动入口、进程残留或反向代理指向不同运行目录。当前不将“263卡片/空周数据”误判为本轮去重修复成功，也不回滚已健康的服务基础状态。

服务入口交叉核验显示，`amazon-listing-web.service`主进程为Node，启动参数指向`/opt/amazon-listing-tool/dist/index.js`，工作目录也为`/opt/amazon-listing-tool`，并由该单元管理。与此同时，本地`dist/index.js`生成时间晚于`weeklyOps.ts`与`parentWeeklyOverview.ts`的最终修改时间，且构建产物中存在`buildParentWeeklyOverview`与`isManaged`标识。因此工件本身包含预期代码。下一步以禁用浏览器缓存的相同已认证tRPC请求核对实际响应，并同时只读检查反向代理的上游监听端口和Web主进程启动时间；只有在确认服务确实未加载新路由时，才执行一次受控重启或回滚，而不猜测式改动数据。

用户登录态在根页面稳定可用。再次直接导航到深层`/ops/products`时浏览器出现偶发超时并重置为空白页；通过应用已经存在的“产品总览”标签返回可重新触发加载，当前显示加载指示器，尚未出现新的错误边界或控制台异常。该浏览器导航波动与后端总览响应内容的问题分开记录，后续仅以加载完成后的同一认证会话响应作为周度数据合同判断依据。

页面重新加载后重复确认：当前选择的是“**MCP父ASIN周报**”系统页签，客户端仅发起`productOps.getProductOverviewWithWeeks`，而不是ERP导入总览查询。接口HTTP 200返回263个产品对象，但全部`weeks=[]`，且全部缺少`isManaged`、`inventory`字段；相同会话中以禁用缓存方式附加参数的重新请求改变了tRPC批处理请求索引，未用于业务结论。前端源码没有覆盖或清空周数组的转换。结合“构建物含新代码、文件哈希一致、响应却是旧合同”的矛盾，当前最高优先级假设是Web进程尚在内存运行原先的程序，即前一轮`systemctl restart`未实际完成或被会话中断打断。下一步只读读取服务的激活时间与主进程启动时间；若早于部署时间，依据已有用户确认执行一次明确的Web服务重启（不动Worker/Scheduler/数据库），然后重新验证同一接口合同。

只读启动时间核验已确认该假设：`amazon-listing-web.service`的`ActiveEnterTimestamp`、`ExecMainStartTimestamp`和主Node进程启动时间均为2026-09-04 17:48:45（中国标准时间），早于本轮已切换编译目录的部署操作约一天。这解释了“磁盘工件为新版本、HTTP健康但总览响应仍为旧合同”的矛盾：前一轮原子切换后的多次Workbench断连导致服务重启命令未完成，旧Node进程继续持有原服务器代码。用户已经明确确认了发布与受管服务重启；下一步只重启`amazon-listing-web.service`，不操作Worker、Scheduler、数据库、迁移或领星调用，随后立即重新检查启动时间、HTTP和已认证总览响应。 

单Web服务重启命令已在Workbench终端提交；终端暂未回显`active`或新的启动时间，且尚未返回新的Shell提示符，因此当前仅记录为“重启进行中或等待完成”，不将其视为成功，也不重复提交重启命令。将等待一次受控超时窗口后读取状态；若仍无回显，改用新的只读终端会话检查systemd状态与最近日志，不操作数据库、Worker或Scheduler。

等待窗口后，Workbench确认单Web服务重启成功：状态为`active`，主进程已从部署前的PID切换为新PID，`ExecMainStartTimestamp`与`ActiveEnterTimestamp`均更新为2026-09-05 17:42:44（中国标准时间）。至此，运行Web进程已确定重新加载当前`dist`工件；Worker、Scheduler、数据库、迁移和领星MCP均未被本次补救操作触碰。接下来返回已认证产品总览，重新加载同一查询并核验周数组、新合同字段、卡片去重及子体数量。

### 统一产品视图发布与受限回归

用户确认后，版本`43f30824`已通过校验工件进行原子发布，旧`dist`保留为`dist.pre-43f30824`，仅重启`amazon-listing-web.service`。随后确认Web、Worker和Scheduler均为`active`，本机`/ops/products`返回HTTP 200，且Web最近十分钟致命错误计数为0。已认证页面显示“统一产品视图”说明条，原先的“MCP 父ASIN周报 / ERP 数据”双页签已消失；该页面仍短暂显示产品骨架屏，而ERP历史查询属于补充数据却被并入首屏加载条件。

对此已实施最小前端修复并发布版本`b658c10c`：总览首屏只等待MCP权威周报查询，ERP历史查询返回后再合并未覆盖自然周。发布采用相同的校验、暂存和原子替换路径，保留`dist.pre-b658c10c`作为直接回滚目录；Web服务新激活时间为2026-09-05 19:03:09（中国标准时间），`/ops/products`本机HTTP 200、近期致命错误计数为0。

青岛已认证浏览器在此次加载门控发布后的页面级验证中发生浏览器子系统连续崩溃，无法再读取渲染后的真实卡片或详情内容；这不是应用控制台错误，也不能作为页面功能成功或失败的结论。服务基础健康、部署工件与入口仍已确认。待浏览器恢复后，仍需在同一登录会话完成三项最终人工/自动复核：总览非骨架且仅一套卡片、任一卡片进入`/ops/products/view/:parentAsin`、子ASIN表的销量/利润/广告/库存与来源标签正确显示。

### 用户截图复验与站点别名修复

用户随后提供的已认证详情截图确认统一产品视图已生效：同一详情页同时呈现来源说明、自然周报表、销量/利润/广告/库存趋势和广告关键词明细；子ASIN表具有销量、销售额、订单利润、广告花费、广告销售额、ACoS、Session、可售库存和在途库存列。日均销量对未满足完整日覆盖的行显示“未提供”，符合不把缺失日期伪造为零的既定语义。

截图也复现了一个剩余身份缺陷：同一店铺、同一子ASIN在`US`和`美国`两个站点文本下各显示一次，造成子ASIN表成员数被放大。它们是同一美国站点的别名，不应视为跨站点实体，也不应把两行的日指标再次相加。

已新增共享的`normalizeMarketplaceCode`规则，并接入ERP历史合并、统一总览、MCP父ASIN周报、日快照父子汇总四条身份键路径。`US`、`美国`、`美国站`与`Amazon.com`现在均归并为`US`；未知站点保持稳定大写值，避免悄然丢弃可审计来源。日级同身份冲突仍按现有MCP优先规则择一，不对指标二次累计。定向回归共17项通过，覆盖别名去重、MCP覆盖ERP同周、跨店隔离及子ASIN指标合同；涉及模块的ESLint和完整生产构建均已通过。

### 站点别名修复的青岛发布结果

用户确认后，版本`bc16fb2b`已作为只含`dist`、`package.json`和锁文件的工件在青岛暂存，远端SHA-256核验通过。发布脚本以原子方式将原`dist`保留为时间戳回滚目录，再替换为新工件；该过程未运行数据库迁移、未写入业务数据、未调用领星MCP。重连后的只读健康检查显示：Web、Worker、Scheduler均为`active`，Web主进程启动时间为2026-09-05 19:41:39（中国标准时间），`/ops/products`本机HTTP响应为200，近20分钟未检出`uncaught`、`fatal`、`TypeError`或`ReferenceError`。

沙箱浏览器随后直接访问产品总览时出现90秒导航超时，替代入口的根页面为白屏且控制台无异常；独立网络的`https://kuahaixing.com/`与`/ops/products`均为HTTP 200。用户此前已提供认证详情截图，证明真实交互会话的统一详情可加载，但该截图发生在别名修复发布前。因此，别名去重的最终页面级证据仍待用户刷新同一详情或已认证浏览器恢复后确认；当前不得仅根据服务健康把页面验收标记为完成。

#### 最终页面截图核验笔记（横向切片1—2）

用户刷新后的青岛认证详情截图为3,114 × 366像素、宽高比约8.51:1，按左至右带重叠切成6段阅读。第1段确认标题显示“子ASIN变体销量（4）”，4条子体均显示为同一店铺和规范站点“3店-US / US”，未再存在“美国”别名行。第2段与第1段重叠的SKU和销量列一致，确认4行最近4周销量分别为306、113、165、8；该段未出现额外重复行。其余列将继续按切片阅读顺序核验。

第3段确认平均日销对四条记录均显示“未提供”，这符合日快照日期覆盖不完整时不虚构日均值的既定语义；销售额列在同一4行均显示数值。第4段确认订单利润、广告花费和广告销售额三列均完整显示4个对齐行，未见因别名合并导致的指标列缺失或额外累加行。

第5段确认广告销售额、ACoS、Session与可售库存均保持4行对齐，ACoS显示为7.5%、22.4%、7.4%、4.4%，Session显示为1,422、783、796、33。第6段确认可售库存、在途库存和数据日期完整显示，日期为2026-09-04（前三行）及2026-08-30（第四行）；库存零值被保留为来源事实，未被转换成缺失。已按6段左至右顺序阅读并核对相邻重叠列，未发现边界重复或遗漏。至此，截图证明站点别名修复已将原先8行的重复子体收敛为4个规范成员，同时保留销售、利润、广告、流量、库存及数据日期的可操作展示。

### 统一产品总览认证页面验收

用户提供的青岛认证总览截图确认页面顶部仅保留“统一产品视图”说明，不再展示“MCP父ASIN周报”与“ERP数据”的双页签。说明条明确同一父ASIN、店铺和站点仅显示一张卡片，MCP父ASIN自然周报优先，ERP只用于补充未覆盖历史周。截图中的两张产品卡片均显示规范站点`US`、`MCP周报`与`ERP历史`来源标记，并提供同一详情跳转入口；未关联人工主档的记录显示“未绑定档案”，不展示可造成写入的主档管理入口。筛选、导入、批量周度同步和库存规划入口均保留在单一总览页。

该截图与前述认证详情截图共同完成了单总览、单详情、来源可见、主档安全降级与US/美国别名去重的页面级验收。部分卡片在当前自然周显示零值，截图无法且不应将其解释为缺失或异常；详情页仍按真实日快照展示指标，周度总览不从日快照重复累计。

## 缺失父ASIN自然周报补齐：首周只读预览（2026-09-06）

用户要求补齐缺失的父ASIN自然周数据后，先对青岛生产库进行了仅日期与计数的覆盖审计：当前MCP父ASIN周报权威事实的连续已覆盖窗口为2026-08-24至2026-08-30；最近26个已完成自然周中，2026-03-01至2026-08-23为待补齐历史自然周窗口。审计未读取或输出店铺、父ASIN、金额、广告词或凭据，也未修改任何周度事实。

以2026-03-01至2026-03-07为首个缺失自然周，已从“领星同步预览”界面对全美国站授权范围发起一次只读MCP预览。浏览器请求在30秒时前端超时并恢复按钮，但生产批次表的只读核验确认服务器已完成并保留批次`#90583`：状态为`ready_for_review`，自然周范围准确，读取672条父ASIN周报，预期店铺日期窗口9个、完成窗口9个、覆盖自然日7天、分页截断0个、失败窗口0个、待复核行0条、字段或身份异常行0条。该结果证明前端超时不等同于领星读取失败；批次已满足父ASIN周报的完整性校验条件，但目前尚未确认或应用，未写入`lingxing_product_weekly`。

随后对2026-03-08至2026-03-14发起同样的全美国站只读预览。页面在读取窗口结束后恢复操作按钮，说明前端请求已结束；但用于确认后台批次的生产只读SQL先后因`ops_external_sync_batches`的混合命名字段（`workspace_id`与数据域列名）不一致而被拒绝。尚未据此判断第二周读取成功或失败，也未触发确认、应用、迁移或业务事实写入；下一步只会先`DESCRIBE`该表确认真实列名后再继续脱敏状态审计。

生产表结构确认后，`ops_external_sync_batches`使用混合列名：`workspaceId`、`data_domain`、`scope`、`summary`等。只读批次摘要确认第二周已正常完成：批次`#90584`为`ready_for_review`，范围2026-03-08至2026-03-14，读取672条周报行，店铺覆盖9/9，覆盖天数7，失败窗口0，分页截断0。最新三条周报批次还显示：`#90583`为2026-03-01至07的同样完整预览；`#90576`为已应用的2026-08-24至30周报。前两条历史预览均未应用到周事实表。

为避免前端30秒等待限制，后续缺失周通过同一受治理`createPreview`接口以单周串行方式发起，始终使用`ALL_US`、`US`和周日开始/周六结束的完整自然周。2026-03-15至21的批次`#90585`读取672条并成功返回。任务处理中途截至2026-06-13已完成12个周窗口，其中11个成功生成预览批次；2026-06-07至13返回系统内部错误，未生成批次、未写入事实，已作为单独失败窗口保留待诊断。其余周仍继续串行预览；本阶段所有调用均为草稿预览，尚无确认或应用动作。

历史预览任务已完成。2026-03-22至2026-08-23共23个自然周中，22周成功生成受治理草稿批次（每周672条读取结果），2026-06-07至13为唯一未生成批次的失败窗口。结合先前完成的2026-03-01至21三周预览，26个待补齐自然周中已有25周处于可审核预览状态，尚无任何历史周被确认或应用。下一步须对25个批次统一进行覆盖、分页、自然周、身份和异常计数的脱敏完整性核验；失败周不随其他完整周自动重试或写入。

对25个`ready_for_review`历史父ASIN周报批次执行只读汇总后，范围覆盖2026-03-01至2026-08-23，合计读取16,494行。仅21个批次满足9/9授权店铺、7天自然周、零失败窗口和零分页截断的完整性门槛；另4个批次累计有4个失败窗口及4个分页截断，均保持草稿隔离。没有待复核行被确认或应用。下一步需要仅按周日期列出该4个不完整批次，再提交21个完整周的最小写入确认；2026-06-07至13无批次失败周继续留在缺口清单，不自动重试。

用户已确认仅应用21个完整周。为避免绕过受治理逻辑，已创建一次性执行器`apply-qingdao-parent-asin-weekly-history-complete.mjs`：默认只读，只有显式`--apply`且完整草稿批次数恰为21时才会串行调用`applyParentAsinWeeklyMcpBatch`。该函数会再次校验批次状态、行校验错误、批内身份唯一性及已有同来源事实冲突，并在事务内仅追加不存在的身份；冲突时停止且不覆盖历史记录。执行器工件SHA-256为`aec7f56ae58f61bb57479b0bb346855b6f9fe8045acce2eb730d4eb5119cbbc9`，临时工件地址为`https://files.manuscdn.com/user_upload_by_module/session_file/310419663030562636/wBVQroIHhGtvhKkA.mjs`。工件不含环境文件、凭据或业务数据。

后续补齐必须继续以“每个自然周一次全美国站预览 → 覆盖/分页/身份/字段校验 → 汇总脱敏结果 → 用户一次性确认 → 幂等应用”的顺序执行。不得以日快照推导或补零替代父ASIN周报，也不得覆盖既有MCP周事实或历史ERP参考记录。

### 历史应用保护性阻断与规范键修复

用户确认应用21个完整周后，一次性执行器先以受管服务环境进行默认预检，确认完整草稿批次数仍为21、范围仍为2026-03-01至2026-08-23、`writePerformed=false`。首次显式应用在第一个批次进入受治理函数时被`父ASIN周报MCP自动应用失败：批次存在重复业务身份`保护性阻断；终端没有显示任何已应用批次结果，未将该批次状态更新为已应用，也未显示事实插入结果。

最初将阻断归因为`US`/`美国`站点别名，但该判断随后被生产草稿直接查询否定。批次`#90583`共672行：`source_data`可识别的站点字段全部为空，`normalized_data.country`全部为`US`，不存在原始`美国`或原始`US`两类值并存。页面侧共享站点规范化仍然有效，但不是本批周事实应用阻断的根因。

为确保应用脚本继承生产数据库连接，已改用受管服务`/etc/amazon-listing-tool/runtime.conf`的`systemd-run`隔离环境，而不读取或打印环境变量值。CommonJS执行器工件已通过语法与SHA-256校验；其默认预检完成后，后续应用须先发布上述规范键修复并再次运行相同的无写入预检，之后才可在用户已确认的21周范围内重试。4个不完整周和2026-06-07至13无批次周继续隔离。

第二次无写入预检确认范围仍严格为21个完整批次、`writePerformed=false`。随后应用再次在首批的严格重复保护前停止；诊断确认未发生部分写入。对批次`#90583`按“工作空间、店铺、父ASIN、规范站点、自然周”聚合后得到336个重复身份组、672条候选行，每组恰好2条；两条候选的`asin`均不等于`parentAsin`，且672条全部是非空单一子ASIN值。336组中`asin`、`sku`、销售、订单、利润、Session、广告花费和广告销售额均存在差异，而来源域/目标字段无差异。由此确认真实结构是**同一父ASIN自然周下的不同子ASIN成员行**，不是可任选一条的重复脏数据。正确修复必须按父ASIN周身份聚合成员：可加指标求和、比率重新计算、子ASIN集合去重保留；不得任选一行、不得将不同来源重复累计。此前针对周报应用的“仅站点别名折叠”不能解决该批问题，后续实现须以该生产证据为准。

### 领星MCP能力纠正与生产发布准备

领星帮助文档截图与青岛真实草稿共同确认：`query_product_performance_asin_lists`直接支持的是SPU/ASIN/MSKU/SKU维度，没有直接提供Amazon父ASIN周汇总。SPU不得等同Amazon父ASIN。系统请求已改为受支持的ASIN周维度，并在应用层依据明确`parentAsin`映射，将不同子ASIN成员聚合为唯一父ASIN自然周事实；可加指标求和，比率由汇总分子分母重新计算，重复子ASIN或重叠成员继续失败关闭。43项相关回归、涉及文件ESLint和生产构建通过，版本为`6a6b5714`。

用户确认发布后，已生成不含环境文件、凭据或业务数据的青岛运行工件和自包含周报执行器，并记录SHA-256校验值。直接访问云助手命令页一度超时为空白，但青岛ECS实例详情页可正常打开，实例保持运行中；尚未替换运行目录、重启服务或写入周事实。后续继续使用已验证的云助手命令路径执行哈希校验、原子发布和默认无写入预检。

实例详情页的“全部操作”菜单已恢复，并明确显示“远程连接 → 发送命令”入口。当前未触发实例重启、停止、密码、网络、磁盘或其他管理操作；下一步仅使用该云助手入口执行用户已确认的工件校验、原子应用代码替换和无写入周报预检。

云助手“发送命令”表单已打开，目标仅为唯一的青岛ECS实例。已写入受控脚本，内容限定为：下载并校验`6a6b5714`运行工件与自包含周报执行器、备份当前`dist/package.json/pnpm-lock.yaml`、原子替换运行产物、重启Web/Worker/Scheduler、执行本机HTTP健康检查，并在受管环境下运行默认无写入预检。当前尚未点击“立即执行”，远端未发生代码或数据变更；提交前继续核验编辑器完整内容与执行按钮状态。

编辑器核验发现，通用输入接口未能向Monaco写入完整命令，编辑器仍只保留默认`#!/bin/bash`首行，执行按钮保持禁用。该尝试未提交远程命令、未替换代码、未重启服务、未写入数据库。下一步改用Monaco原生粘贴事件写入已校验的单行Base64包装命令，并在提交前再次读取完整长度与首尾标记。

后续核验确认，云助手内嵌Monaco同时拒绝通用输入与原生`paste`事件，页面也未暴露可直接设置模型内容的`window.monaco`接口；编辑器始终只有12字符默认首行，执行按钮持续禁用。以上尝试均未提交远程命令。为避免继续与前端编辑器兼容问题纠缠，发布通道改回已验证的Workbench终端原生粘贴方式，并保持相同工件、哈希、备份、回滚和默认无写入预检边界。

Workbench实例直达页现已恢复到免密登录对话框，目标为青岛唯一ECS实例，用户名为`ecs-user`，免密连接状态正常。尚未登录终端或执行命令；登录后将仅粘贴此前已通过Shell语法检查的单行Base64包装命令。

首次点击登录时页面加载导致按钮索引变化，未进入终端；随后旧索引已失效。该过程没有执行任何远程命令或产生运行变更。下一步重新获取当前页面元素后点击现行登录按钮。

Workbench免密终端现已成功连接，提示符为青岛实例的`ecs-user`会话；已定位唯一的`xterm-helper-textarea`原生输入控件。当前仍未粘贴或执行发布命令。下一步通过终端原生粘贴事件输入已校验的单行Base64包装命令并按回车提交。

已通过终端原生粘贴事件输入单行Base64包装命令，终端可见该命令从提示符开始连续换行显示，末端仍为待提交状态，尚无哈希、服务状态或预检输出。下一步仅按一次回车执行，严禁重复粘贴或并发运行。

首次受控执行已返回：运行工件与执行器两个SHA-256均校验为`OK`，暂存包也通过`dist/index.js`存在性及父级聚合符号检查；但在创建当前运行包备份时，`tar`报告目标归档无法打开。失败陷阱随后尝试回滚，但因归档并未成功创建而再次失败，并已执行了陷阱中的`rm -rf /opt/amazon-listing-tool/dist`。因此当前风险是运行中的Node进程仍在内存，但运行目录`dist`已被删除；服务尚未由该脚本重启，预检未运行，业务数据没有写入。立即恢复措施为：从已验证且仍位于暂存目录的`6a6b5714/dist`原子移回运行目录，随后重启三项服务并执行本机健康检查；不再重跑原脚本。

最小恢复命令已经单次提交：它先确认运行目录`dist`仍不存在、暂存`dist/index.js`存在，再将暂存目录移回运行目录，重启Web/Worker/Scheduler并做三项`is-active`与本机HTTP检查。当前终端尚未返回最终`RESTORE_OK_VERSION_6a6b5714`，因此下一步仅等待结果，不重复提交。

最小恢复命令已成功将已校验的`6a6b5714/dist`移回运行目录；随后普通用户直接调用`systemctl restart`被Polkit认证要求阻断并超时，命令未返回健康成功标记。为避免交互式密码和继续阻塞，已在提示符粘贴`sudo -n systemctl restart`的非交互版本，后接三服务`is-active`与本机HTTP检查；该命令尚未按回车。若实例未配置免密sudo，它会立即失败，不会再次等待认证。

非交互sudo命令提交后Workbench会话立即断开，终端未保留可见的成功或失败输出；控制台实例仍显示运行。当前已返回Workbench免密登录页，下一步重新连接后只读检查`dist/index.js`、三项服务状态、进程启动时间及本机HTTP，不重复执行重启，先判断上次命令是否实际完成。

重新登录时页面再次处于动态加载阶段，登录按钮索引在点击间发生变化，尚未重新进入终端或执行任何新命令。下一步刷新当前页面元素并使用最新登录按钮继续只读核验。

Workbench已自动完成新的免密会话连接。已在提示符粘贴只读恢复核验命令：确认运行目录`dist/index.js`存在且包含`aggregateChildWeeklyFactsByParent`版本标识，读取三项systemd服务的`is-active`与`ActiveEnterTimestamp`，并请求本机HTTP状态。命令尚未按回车，不包含重启、数据库或业务写入。

只读恢复核验已实际完成并在终端回显：`DIST_VERSION_6a6b5714_OK`；Web、Worker、Scheduler三项服务均为`active`；本机入口返回`HTTP_200`。这确认青岛运行目录现为父ASIN成员聚合修复版本且服务可用。核验输出完成后Workbench再次断开，未影响实例运行。至此仅完成代码恢复与健康检查；历史周预检、21周事实应用和任何业务数据写入均尚未执行。

现已重新发起Workbench免密连接，准备把历史周**无写入预检**作为与发布分离的独立步骤运行。预检将复用已下载且哈希验证通过的受治理执行器，默认不带`--apply`，通过受管运行环境启动；当前尚未提交预检命令。

Workbench已连接，预检命令现已完整粘贴但尚未提交。该命令先再次校验远端执行器SHA-256，再使用非交互sudo调用受管一次性单元；参数中明确没有`--apply`，因此只允许输出21个已确认完整批次的候选聚合与`writePerformed=false`等只读结果。下一步仅按一次回车执行。

无写入预检命令已提交，终端先回显远端执行器SHA-256校验为`OK`，随后Workbench连接在一次性单元输出前断开。当前没有预检成功或失败结论，也没有看到`PREFLIGHT_COMMAND_OK`；由于命令未传`--apply`，即使一次性单元已启动也不允许业务事实写入。下一步重新连接后只读查询`parent-weekly-preflight-6a6b5714-r2`单元状态与日志，不重复启动预检。

断开前的终端缓冲现已显示首轮预检结果：一次性单元在加载阶段以`MODULE_NOT_FOUND: Cannot find module 'dotenv/config'`退出，业务校验逻辑未启动，`PREFLIGHT_COMMAND_OK`未出现。根因是哈希一致的执行器仍位于`/tmp`，Node按脚本文件路径解析依赖，无法访问应用目录下的`node_modules`；这不是数据冲突，也未产生任何事实写入。修复方式是将同一哈希执行器以600权限复制到`/opt/amazon-listing-tool/scripts`，使其按应用目录解析依赖，再以新的单元名重跑仍不带`--apply`的预检。

Workbench现正重新建立免密会话，尚未执行新命令。下一步只做两件事：把已验证哈希的执行器复制到应用`/scripts`目录；使用新的单元名运行不带`--apply`的无写入预检。不会重启服务或读取新的MCP数据。

Workbench已连接，修正后的预检命令已完整粘贴但尚未提交。命令将把`/tmp`中的同一执行器以600权限安装到应用`/scripts`目录，并对安装前后文件分别执行SHA-256校验；随后使用新的一次性单元名运行，仍明确不传`--apply`。下一步仅按一次回车。

修正后的无写入预检命令已提交，Workbench在终端输出前再次断开；当前未取得成功或失败结论。由于命令不带`--apply`，即使一次性单元已运行也不允许事实写入。下一步重新连接后只读读取`parent-weekly-preflight-6a6b5714-r3`状态与日志，禁止重复启动该预检。

终端缓冲已显示第二轮命令结果：源执行器哈希校验通过，`sudo install -m 600`也已完成；后续由普通`ecs-user`直接读取目标600权限文件进行二次哈希时被拒绝，命令链因此在启动一次性单元之前停止。该结果说明执行器已经安全安装到应用`/scripts`目录，但预检仍未启动，业务数据仍无写入。下一步只需用`sudo sha256sum`校验目标文件，再以新单元名运行不带`--apply`的预检。

Workbench再次进入免密连接加载阶段，尚未执行新命令。下一步命令将只用sudo读取已安装执行器计算SHA-256并与固定期望值比较，校验成功后才运行新的无写入预检单元。

Workbench已连接，最终修正的预检命令已完整粘贴但尚未提交：通过sudo计算600权限目标执行器的SHA-256并严格与固定值比较，只有一致时才以新单元名启动；调用参数仍没有`--apply`。下一步仅按一次回车。

最终无写入预检已成功完成：执行器目标文件哈希通过；输出`action=dry_run_complete_parent_weekly_backfill`、日期范围`2026-03-01`至`2026-08-23`、`completeBatchCount=21`、`expectedCompleteBatchCount=21`、`writePerformed=false`，并出现`PREFLIGHT_COMMAND_OK`。但是，预检候选日期集合与此前人工记录的隔离清单存在一项不可忽略的差异：本次候选包含`2026-07-19`，同时未列出`2026-05-31`；旧记录则将`2026-07-19`列为不完整周，且未把`2026-05-31`列入异常清单。虽然总数仍为21，但范围成员不完全一致，因此暂不执行`--apply`。下一步先以只读方式核对这两个周及全部26周的批次状态、店铺覆盖、失败/截断计数和草稿行数，确认实际21周集合后再决定是否应用。

为解决上述日期成员差异，已生成一次性只读审计脚本，脚本只查询批次元数据和行数，不读取店铺、ASIN、金额或凭据，也不执行写入；本地语法检查通过，SHA-256为固定值。Workbench提示符现已粘贴“下载、源文件哈希、600权限安装、目标文件sudo哈希、受管只读执行”命令，但尚未提交。下一步仅按一次回车运行该审计。

只读批次覆盖审计已成功完成并返回`READ_ONLY_BATCH_AUDIT_OK`、`writePerformed=false`。范围内实际有25个草稿批次：21个批次满足9/9店铺、7天、9/9窗口、零失败、零截断且均为672行；4个不完整批次分别为`2026-04-12`（8/9店铺、1失败、1截断、554行）、`2026-05-24`（8/9、1失败、1截断、650行）、`2026-05-31`（8/9、1失败、1截断、506行）和`2026-08-16`（8/9、1失败、1截断、672行）。`2026-06-07`没有生成批次，继续保持缺口。`2026-07-19`批次实际为9/9、零失败、零截断、672行，属于完整周；此前调查记录把它列为异常周属于转录错误。由此，执行器预检的21周集合与数据库实际完整性审计一致，可以在用户既有授权范围内以`--apply`串行调用现有受治理幂等应用函数；4个不完整周和1个缺批周将保持隔离。

用户已授权的显式`--apply`命令现已单次提交，执行器SHA-256再次通过并回显`RUNNER_HASH_OK`；受管一次性单元正在按21个完整周的自然周顺序串行调用既有事务化、幂等应用函数。当前尚未返回`writePerformed=true`、批次结果或`APPLY_COMMAND_OK`，因此不得重复启动或并发执行。

显式应用已被保护性规则阻断，终端返回“父ASIN周报MCP自动应用失败：同一父ASIN周内存在重复子ASIN成员”，且未出现`writePerformed=true`或`APPLY_COMMAND_OK`。当前停止所有写入尝试，不重跑应用。下一步先只读确认失败批次、已应用批次数和父周事实增量，排除部分写入；随后对首个失败批次仅输出脱敏重复模式计数，检查重复是同一子ASIN的真实重复、映射字段选择错误，还是跨来源/别名造成的成员身份冲突。

已生成并上传重复成员与部分写入的只读脱敏诊断脚本，本地语法检查通过且固定SHA-256已记录。Workbench现已返回免密登录页；诊断脚本尚未在生产执行。下一步重新连接后只查询首个失败批次的`asin`/`asins`字段形态、重复成员计数，以及本轮25个批次的状态、来源事实和确认记录聚合数。

Workbench已重新连接，当前提示符空闲；重复成员与部分写入诊断脚本仍未在生产执行。下一步仅下载并校验该脚本，以受管环境运行只读查询，不再尝试应用。

重复成员与部分写入的只读诊断命令已提交，源文件与安装文件哈希检查已通过并回显`DUPLICATE_AUDITOR_HASH_OK`；受管一次性单元正在执行只读聚合查询。当前尚未返回诊断JSON或`READ_ONLY_DUPLICATE_AUDIT_OK`，不进行任何写入或重复执行。

重复成员与部分写入审计已成功完成并返回`READ_ONLY_DUPLICATE_AUDIT_OK`、`writePerformed=false`。首个失败批次共672行，但只有336个不同`entity_key`；336个业务键全部各出现2次。以规范父ASIN、店铺、站点、自然周和`normalizedData.asin`组成的成员键统计同样得到336个重复组、每组恰为2行。672行的单值`asin`均非空、均不等于父ASIN，`sourceData.asin`与`normalizedData.asin`也全部一致；`normalizedData.asins`数组在所有行中均不存在。说明失败并非`asins`集合误用，而是上游同一个子ASIN业务键确实生成了两条独立行。

部分写入核验同时确认：本轮25个候选批次状态仍全部为`ready_for_review`；以本轮批次ID作为`source_batch_id`的父周事实批次数和行数均为0；`system_auto_apply`确认批次数和记录数均为0。因此显式应用在首批写入前即失败，**没有任何部分写入**。下一步需继续只读判断每对重复行是否由不同SKU/MSKU或其他明确来源维度区分；只有存在可证明的互斥成员维度时才能安全合并指标，否则必须继续失败关闭。

重复行等价性只读审计命令已提交，脚本源文件与安装文件哈希均通过并回显`EQUIVALENCE_AUDITOR_HASH_OK`；受管单元正在比较336对行的完整归一化载荷、原始载荷、SKU/MSKU、记录ID、指标向量和字段集合。当前尚未返回诊断结果，不进行写入。

重复行等价性审计已成功完成并返回`READ_ONLY_EQUIVALENCE_AUDIT_OK`、`writePerformed=false`。336个重复成员组全部为2行：两行的规范SKU、原始MSKU、记录ID和字段集合在336组中全部相同，因此并非不同SKU/MSKU的合法细分；但完整归一化载荷与完整原始载荷在336组中均无一完全相同，指标向量有193组相同、143组不同。结论是：这些成对行既不能作为“不同子ASIN成员”求和，也不能统一当作纯传输副本折叠；143组存在真实数值差异，继续触发失败关闭是正确的。需要进一步只读识别成对行中哪个原始维度不同，以及它们是否对应同一周的不同日/不同日期口径、重复页或其他来源分层；在确认唯一合法去重/汇总规则前不得应用21周。

已提交第三个只读差异字段审计，源文件与安装文件哈希均通过并回显`FIELD_DIFF_AUDITOR_HASH_OK`；该审计只统计哪些原始/归一化字段不同及指标比较方向，不输出字段值或业务明细。当前尚未返回结果，不进行写入。

差异字段审计已成功完成并返回`READ_ONLY_FIELD_DIFF_AUDIT_OK`、`writePerformed=false`。最关键证据是：336对重复行的原始`rdate`与`rweek`在**全部336对**中都不同，而归一化层的`reportDate`也在全部336对中不同；SKU、MSKU、记录ID和父/子ASIN身份则相同。销售、利润、流量、广告等指标只在部分配对中不同，说明两行本质上来自两个不同的源周/日期桶，而非同一周的不同子ASIN或不同SKU。

当前代码在父ASIN周预览中只识别`week_start_date/weekStartDate/start_date/period_start/report_start`等字段，未识别领星实际返回的`rdate/rweek`；随后又把所有行的`week_start_date/week_end_date`回退并强制写成请求范围，导致两个源周桶被错误压到同一自然周身份。这是第二层根因。下一步继续只读核对首批`rdate/rweek`的唯一值与请求周边界关系，确定应严格保留哪个源周桶、如何解析其自然周；在修复预览筛选和历史草稿应用防护前仍不写入。

源周桶只读审计命令已提交，脚本源文件与安装文件哈希均通过并回显`SOURCE_BUCKET_AUDITOR_HASH_OK`；当前正在仅输出请求范围、`rdate/rweek`唯一桶及行数，不读取ASIN、店铺或金额。尚未返回结果，不进行写入。

源周桶审计已成功完成并返回`READ_ONLY_SOURCE_BUCKET_AUDIT_OK`、`writePerformed=false`。批次90583的请求范围为`2026-03-01`至`2026-03-07`，源响应严格分成`2026-03-01~2026-03-01`与`2026-03-02~2026-03-07`两个桶，各336行；336个子ASIN身份均恰好覆盖这两个桶。这证实领星周视图按周一至周日切分，而该历史批次请求跨越了两个自然周。

进一步复核一次性历史预览脚本发现，它明确以“周日至周六”生成区间（`completeSundaySaturdayWeeks`、首日必须为周日、结束日为首日+6天），与产品既定且用户明确确认的“周一至周日”自然周口径冲突。因而现有21个所谓“完整周”只代表店铺/分页读取完整，不代表业务自然周正确；即使把1日与6日片段安全相加，也会生成错误的周日至周六事实。现已停止对现有21批的再次应用；全部批次仍无父周事实写入。下一步只读评估能否从相邻已预览批次的源分片重组正确的周一至周日窗口；若不能在不猜测、不补0的条件下证明完整，则必须重新预览正确自然周并取得新的明确授权后才可写入。

本地已完成周周期安全修复：`normalizeRow`现在优先解析领星`rweek/rdate`真实源周期，父周草稿身份增加子ASIN与源周期起止日；历史预览脚本改为周一至周日的26个完整自然周；应用器要求显式提供经用户确认的`APPROVED_PARENT_WEEK_STARTS`精确清单。预览完整性和应用函数均在任何事实写入前检查目标周必须为周一至周日、每行真实源周期必须与目标周完全一致、子ASIN周身份必须唯一。旧周日至周六批次因此只能被拒绝，不能通过合并1日+6日分片伪造成自然周事实。

本地验证结果：父周核心定向回归61项通过，涉及文件ESLint通过，两个一次性脚本语法检查通过，历史预览干运行输出26周，首周为2026-02-23至2026-03-01、末周为2026-08-17至2026-08-23；生产构建与bundle预算检查通过。完整TypeScript仍有169项既有诊断，与修复前数量一致；当前匹配本轮文件路径的诊断均位于既有未改动代码区域。单独扩展回归还观察到ASIN日分页集成用例仍期望5,000全局行上限而实现按窗口累计至6,000，以及MCP action adapter旧Schema夹具不匹配，这两项与父周源周期修复无关，未在本轮扩大范围修复。

版本`efe269c2`已按“仅替换已构建`dist`”方式受控发布至青岛：发布工件SHA-256为`f50818a60637ad73b491c643f0b0c55fd6b1739112ad09e81e770d2c22f468ec`，发布前完整备份当前`dist`，替换后Web、Worker、Scheduler均为`active`，本机HTTP返回200，运行包存在`lingxing_asin_weekly_source_period_v2`标识。发布单元返回`DEPLOY_OK`，未运行迁移、MCP读取或业务数据写入。

发布后使用哈希固定的独立只读验证器，在受管运行环境中逐批调用新版完整性规则。脱敏结果为：25个既有`ready_for_review`父周批次、16,494行草稿，25批均以周日开始、0批以周一开始，`acceptedCount=0`、`rejectedCount=25`、`writePerformed=false`；其中21批因不是周一至周日自然周被阻断，4批因店铺覆盖不完整被阻断。该结果同时证明旧21批不能再次应用，且截至本次预检仍未产生父周事实写入。下一步必须按正确的周一至周日范围重新读取MCP并生成新草稿，再向用户提交新的完整周/异常周精确清单确认。

用户确认后，青岛以单一后台任务按周一至周日串行重新预览2026-02-23至2026-08-23共26个完整自然周。任务成功结束，`previewOnly=true`、`writeCalls=0`：25周生成审计草稿，2026-06-01至2026-06-07因领星`get_my_sids`只读Schema不匹配未生成批次。预览过程未调用父周事实应用器。

完成后使用独立只读审计器逐周复核全店覆盖、7日范围、失败窗口、分页截断和真实源周期。新精确清单如下：

- 完整23周：2026-03-02、03-09、03-16、03-23、03-30、04-06、04-13、04-20、04-27、05-04、05-18、05-25、06-08、06-15、06-22、06-29、07-06、07-13、07-20、07-27、08-03、08-10、08-17开始的自然周。
- 隔离2周：2026-02-23至03-01、2026-05-11至05-17；两周均为9家授权店铺中完成8家，各有1个失败窗口与1次分页截断，禁止部分写入或填0。
- 缺失1周：2026-06-01至06-07；本轮未生成批次，保持缺口且不自动重试。

只读审计返回`completeWeekCount=23`、`isolatedWeekCount=2`、`missingWeekCount=1`、`writePerformed=false`。因此原先针对旧错位21周的应用授权不再适用；必须向用户提交上述新清单并取得明确确认，才能使用精确周清单保护串行、幂等应用23个完整周。
