# 领星每日自动应用首轮运行审计（2026-08-26）

用户已明确授权手动触发一次`product_performance_daily`计划，使用已启用任务`Gu5NVZ7EUCj3XERAfVUHdQ`的同一运行服务、同一店铺范围、同一完整性校验与同一自动应用路径。运行范围为美国站9个授权店铺、报告日`2026-08-25`。

## 运行结果

| 项目 | 结果 |
| --- | --- |
| 首次草稿批次 | #30005，541行，9个店铺和9个店铺日期窗口均完整，无分页截断；因`orderProfit`为负被旧校验误拒绝，未确认、未应用 |
| 根因 | 真实亏损会产生负`orderProfit`；它是有意义的财务事实，不是非法负数 |
| 修复 | `orderProfit`仅校验有限数值；销量、订单、销售额、广告花费/销售/订单、Session、点击、曝光和退货量仍拒绝负值与非有限值 |
| 修复后运行 | 批次#30006状态`applied`，541行全部应用，跳过0行 |
| 运行追溯 | `runKey=daily:2026-08-25`，Trace=`ops_lingxing_sync_1787729901909_1`，批次摘要保留9个Tool Run引用 |
| 日快照写入 | 导入批次#690001，新增541条`lingxing_mcp`日快照（报告日`2026-08-25`） |
| 周度边界 | `lingxing_product_weekly`中`import_id=690001`为0；本次不触发每周父ASIN汇总，也不写入库存、广告或Listing业务表 |

> 该自动应用仅在全店/全日期窗口完整、无截断、身份无重复、字段有效且异常跃升检查通过后执行。今后北京时间17:00的每日触发对同一`runKey`将幂等跳过，不会重复追加。
