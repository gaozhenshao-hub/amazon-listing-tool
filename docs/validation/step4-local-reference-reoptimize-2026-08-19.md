# Step4 本地参考图单图优化生产复验（2026-08-19）

## 复验路径

在生产环境 `空气套件` 项目（`projectId=90001`）的 Step4 主图卡片中，保留已上传构图参考图与备注，点击“根据参考图和备注重新优化构图和效果方案”。

## 结果

加载态正常出现，页面未再出现截图中的 `NotFoundError: Failed to execute 'removeChild' on 'Node'` 崩溃，也没有落入全页错误边界。说明稳定卡片键与固定状态文本容器已覆盖原始的异步渲染崩溃路径。

但AI结果回写后，页面重新水合为所有图片“已锁定/解锁此图”状态，且主图本地构图参考图与备注未在锁定展示中保留。这个状态覆盖问题与本次 DOM 崩溃不同，不能将本次验收标为完整通过；需要继续定位单图优化路由回写会话快照与前端水合优先级。

## 状态恢复与修复后复验准备

定位确认：单图优化路由此前把已有参考图对象中的历史 `isLocked` 字段原样写回，同时将会话级 `step4Confirmed` 设为 `0`，形成状态矛盾。已发布修复会在写回前清除全部历史逐图锁定快照，并保留本地图片、备注、知识库选图和目标图片身份。

本次复验产生的空气套件测试记录已安全恢复为未确认状态；刷新生产页面后，主图构图参考图、构图备注、编辑字段和“根据参考图和备注重新优化构图和效果方案”入口均重新可见。下一步将使用同一入口验证新回写不再覆盖状态。

再次发起复验时，浏览器页面在交互状态刷新后重新进入逐图锁定展示。需要先核对本次请求是否真正抵达新发布的后端，及其写回的 `isLocked` 字段值，再判断是发布资源未切换、回写修复未生效，还是前端在请求完成前采用了旧缓存快照。该现象暂不作为修复通过证据。

数据库核验显示：最新会话仍为 `step4Confirmed=0`，主图 `isLocked=false`，本地构图参考图URL仍存在；因此锁定展示是浏览器在一次失效交互后的临时旧状态，不是本次新后端写回。以缓存隔离参数重新载入生产页面后，主图已稳定显示本地构图参考图、备注编辑入口和单图优化按钮。现可按正确的最新按钮索引进行真实回归。

最新生产检查点发布后再次刷新，空气套件Step4仍以未确认状态加载，主图本地构图参考图及单图优化入口可见。下一次测试将向主图填写明确的测试备注并执行优化，以验证备注既进入皇帝Skill上下文，又能在回写后保存。

当前验收结论：原始本地参考图优化路径已不再进入 `removeChild` 全页错误边界；Step4单图回写的锁定清理与本次输入备注优先回填已发布，且通过17项定向回归、ESLint及生产数据库快照核验。空气套件测试会话在多次页面状态切换后出现临时的历史展示快照；其持久化记录保持未确认、无逐图锁定字段、并保留本地构图参考图。完整的“填写新备注 → 真实皇帝Skill优化 → 刷新后仍显示新备注”交互演练暂保留为后续稳定会话回归项。

后续定位确认该临时展示来自前端水合合并逻辑只接受“服务端锁定更多图片”的快照，而不会接受服务端已解锁快照；已调整为逐图确认数只要发生变化即采用服务端快照，并补充解锁方向的单元测试。青岛ECS具备约6.6GB可用内存，可用于后续高内存前端构建；但当前Docker Hub基础镜像拉取受网络超时阻断，待配置用户账号专属ACR镜像加速器后执行构建和最终生产交互验证。

最新生产资源切换后，直接以浏览器登录态读取 `imageWorkflow.getSession(projectId=90001)`：响应体包含主图本地构图参考图URL，且完整响应不含任何 `"isLocked":true` 字段。服务端会话快照与数据库一致，均为未确认、无逐图锁定状态；后续交互验证需以独立浏览器会话排除应用内多标签缓存干扰。

## 最终生产回归通过

在空气套件（`projectId=90001`）的主图中，使用已上传的本地构图参考图与备注“保留前中后错落层次，突出蓝色管材与金属接头对比。”执行了真实的“根据参考图和备注重新优化构图和效果方案”。皇帝 Skill 成功返回新的构图、效果与设计师注意事项；页面没有出现 `removeChild`/意外错误，主图继续显示“确认此图”入口而非错误锁定状态。

回归完成后，数据库中的 `image_workflow_sessions.id=780001` 保持 `step4Confirmed=0`、主图 `isLocked` 为空，并持久化保留原本地构图参考图URL与上述备注。生成的设计师注意事项明确引用了“蓝色管材与金属接头对比”，证明本次输入已进入皇帝 Skill 上下文。最新生产前端已使用青岛ECS高内存构建产物切换，相关DOM稳定、历史任务水合、锁定快照与备注回填修复均已上线。
