# 青岛广告关键词日常任务：2026-09-02 自然窗口审计与修复记录

## 审计范围

本记录只覆盖青岛独立站在北京时间 2026-09-02 16:40 的广告关键词日常任务自然运行，以及北京时间 17:05 生成的脱敏聚合审计快照。审计过程未调用领星 MCP、未手动重跑任务、未更改任何广告事实或异常批次。

## 自然窗口结果

广告任务已由既有本机 Scheduler 单实例触发，任务投影的运行次数从 1 增至 2，并将下一次运行投影到北京时间 2026-09-03 16:40。读取与完整性校验已完成，但自动确认阶段失败，因此该自然窗口没有将异常或未确认行写入广告关键词事实表。

| 核验维度 | 脱敏结果 | 结论 |
| --- | --- | --- |
| Scheduler / Web / Worker | 均为 `active/running` | 核心服务健康 |
| 历史补读实例 | 0 个活动进程，补读服务为 `inactive/dead` | 未产生第二补读实例 |
| 日常广告任务 | 已启用；自然窗口后状态为 `failed` | 失败被记录，未伪造成功 |
| 批次汇总 | 530 个 `applied`、15 个 `ready_for_review` | 待复核批次仍隔离 |
| 行级汇总 | 626,850 个 `applied`、3,665 个 `needs_review`、34,929 个 `new` | 异常与未确认行未自动写入事实表 |
| 已应用批次覆盖 | 2026-05-11 至 2026-08-30 | 历史补读覆盖保留且未被覆盖 |

## 根因与修复

Scheduler 日志显示失败发生在自动确认接口的输入校验：跨多个已授权广告 Profile 汇总后的有效行数超过了旧的 5,000 条 `selectedRowIds` 限制。此问题发生在读取和完整性校验之后；系统以失败状态保留批次，未进入广告事实写入。

修复将受控确认总量上限调整为 **200,000** 条，以覆盖多 Profile 的每日汇总，同时将选择状态更新固定按 **每 5,000 条**分块执行。该修改不放宽广告异常校验、重复身份阻断、待复核隔离或应用前确认边界。

| 验证项 | 结果 |
| --- | --- |
| 定向 Vitest | 22 项通过，包含 5,001 条确认行的分块更新回归 |
| 定向 ESLint | 通过 |
| 青岛生产构建 | 通过 |
| Scheduler 重启后状态 | `active`，0 次重启 |
| 生产源码核验 | 大批量确认上限已加载 |
| 完整 TypeScript 编译 | 未通过，项目存在约 200 项既有类型错误；本轮新增行未出现在编译错误尾部，未将全量类型检查误报为本轮通过 |

## 后续真实验收

青岛服务器已安排唯一的一次性只读审计快照，于北京时间 2026-09-03 17:05 执行。它只读取服务状态、任务投影、批次/行状态和已应用日期范围，不调用领星、不写入数据库，也不创建第二个业务触发器。下一次自然广告任务仍由既有 Scheduler 在北京时间 16:40 自行执行；届时将核验真实 Run/Trace、批次应用和异常隔离结果。

## 用户授权手动验证（2026-09-02）

在用户明确授权后，系统通过一次性单实例执行器复用了正式广告任务 UID 和同一套读取、分页、完整性校验、确认与应用链路。该执行器没有新增周期触发器，并在结束时释放数据库连接。

本次验证未发生大批量确认参数超限，证明 200,000 条确认总上限与每 5,000 条分块选择状态更新已进入运行路径。但是，7 个授权广告 Profile 中有 1 个在官方 MCP 的目录查询阶段返回了无效或不匹配的只读 Schema；系统因此按失败关闭原则阻断自动应用。

| 核验维度 | 脱敏结果 | 结论 |
| --- | --- | --- |
| 手动验证实例 | 已结束，退出码为失败 | 失败审计被保留 |
| Profile读取覆盖 | 6 / 7 完成；1 个Schema异常 | 未将不完整窗口视为完整 |
| 最新批次 | `ready_for_review` | 留待人工复核 |
| 最新批次行 | 8,201 个 `new`、44 个 `needs_review` | 未确认行与异常行继续隔离 |
| 2026-09-01广告关键词事实 | 0 条 | 未产生部分写入 |
| Web / Worker / Scheduler | 均为 `active` | 核心服务未受影响 |

> 上游Schema异常属于外部输入不完整，当前正确处理方式是维持批次待复核并等待下一自然窗口，而不是降低Schema契约、自动重试或人工绕过校验。2026-09-03的自然窗口和17:05只读快照将继续验证稳定性。
