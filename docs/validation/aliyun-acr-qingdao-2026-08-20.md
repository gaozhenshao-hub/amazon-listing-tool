# 阿里云 ACR 青岛镜像准备记录（2026-08-20）

已在浏览器中以用户主账号登录阿里云，并进入华北1（青岛）ACR控制台。

| 项目 | 结果 |
|---|---|
| 用户提供的Docker Hub加速器 | 已加载到ECS Docker守护进程，但`node:22-bookworm-slim`拉取仍返回Docker Hub `not found`，不再重复重试Docker Hub。 |
| ACR公共制品中心 | 官方`alinux3/node`可用，但仅列出Node `20.16`（以及旧版16.17.1），不满足应用保持Node 22的运行基线。 |
| ACR实例 | 青岛实例列表仅展示“创建企业版实例”，无可直接使用的个人版Node 22订阅入口。 |
| 用户授权 | 用户已确认可创建ACR托管实例并订阅`node:22-bookworm-slim`与`mysql:8.4`。 |

后续操作必须保持Node 22，不采用未验证的Node 20替代。创建企业版实例/订阅具体镜像前，应在控制台确认实例规格、计费与镜像同步设置；成功后将Compose中的基础镜像改为ACR仓库地址，并在ECS仅拉取和验证镜像，随后进入隔离MySQL恢复验证。
