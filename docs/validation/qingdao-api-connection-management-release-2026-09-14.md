# 青岛独立站受控 API 连接管理后台发布记录

**日期：** 2026-09-14  
**发布版本：** `b68495a0`  
**范围：** 仅发布受控第三方API连接管理后台代码；不执行数据库迁移、不写入或读取任何密钥、不调用Provider/ERP、不创建Heartbeat计划。

## 已执行的发布门禁

发布包仅含已完成本地构建与隐私审计的`dist`。远端先下载发布脚本并完成SHA-256校验，脚本再下载构建包、校验SHA-256、验证三个启动入口存在后才继续。切换前，旧`dist`与发布标记已进行版本化备份；新`dist`通过目录级原子移动替换；任何命令、服务或HTTP检查失败均会自动恢复备份构建产物并重启三个服务。

## 远端终态

| 核验项 | 结果 | 证据 |
|---|---|---|
| 发布脚本SHA-256 | 通过 | 远端输出文件校验`OK` |
| 构建产物切换 | 通过 | 发布标记为`b68495a0` |
| Web服务 | active | 发布脚本`SERVICES_ACTIVE`门禁 |
| AI Worker服务 | active | 发布脚本`SERVICES_ACTIVE`门禁 |
| Scheduler服务 | active | 发布脚本`SERVICES_ACTIVE`门禁 |
| 本机HTTP | 通过 | 发布脚本`LOCAL_HTTP_OK`门禁 |
| 最终标识 | 通过 | `API_CONNECTIONS_RELEASE_OK:b68495a0` |

## 明确保留的边界

生产环境尚未在新后台保存、轮换或校验任何Apify、领星、赛狐密钥。价格/Offer/BSR与关键词排名的资格Run仍为0，尚未产生Provider费用；未创建Heartbeat计划；赛狐仍因缺少经确认的官方受限API合同而保持失败关闭。

## 发布后页面可达性与密钥非回显验证

使用已登录的超级管理员会话访问`https://kuahaixing.com/settings`，页面成功加载“API连接管理”页。三类连接均可见，但仅显示脱敏连接状态、字段状态来源、密钥版本与最后替换时间；所有可编辑字段均是空白`password`输入框，未展示、掩码回填、导出或推断任何历史密钥。Apify卡片提供“无费用轻量校验”按钮，领星与赛狐的各自门禁说明正常显示；本次页面验证未填写字段、未点击保存/校验/重加密，也未产生Provider费用。

随后，已登录超级管理员会话触发了Apify“无费用轻量校验”。该动作仅允许账户身份端点检查，设计上不运行Actor、不创建资格Run或业务采集。截至本记录更新时，页面仍显示校验进行中，尚未出现成功或失败终态；因此不将Apify记录为已校验，也不启动任何付费资格任务。

## Apify轻量校验超时诊断与本地补丁

页面轻量校验最终返回固定脱敏类别`timeout`。在青岛服务器上执行的无令牌只读网络诊断证明`api.apify.com`可通过IPv4解析和HTTPS访问；账户端点在未携带认证的情况下约1.2秒返回预期HTTP 401。因此，网络不可达不是根因，且该诊断不读取配置、令牌或业务数据。

本地补丁将账户检查从全局`fetch`改为受控`node:https`请求：固定`family: 4`、10秒超时、`Authorization: Bearer`请求头认证、URL不含令牌，并在非2xx或超时时保持固定失败关闭。10项受控API连接定向测试、定向TypeScript/ESLint及生产构建/Bundle预算均通过。补丁尚未发布青岛；发布后必须重新执行无费用轻量校验，通过前不得创建两项Provider资格Run。

## IPv4补丁发布单元（进行中）

用户已单独确认以无迁移方式发布补丁`800fd8dc`。青岛Workbench已恢复至目标ECS，远端下载发布脚本后的SHA-256校验返回`OK`，脚本已启动。当前尚未收到`SERVICES_ACTIVE`、`LOCAL_HTTP_OK`或最终成功/回滚标识，因而本记录不提前将生产补丁标记为成功。该单元不写入或读取密钥、不执行DDL、不调用Provider/ERP，也不创建Heartbeat或资格Run。

随后，Workbench原发布终端与独立只读终端均报告远程连接断开。断开前只观察到脚本文件SHA-256校验`OK`，未观察到构建切换、服务重启、健康检查、成功或回滚标识。为避免并发和重复发布，未再次提交发布命令；恢复终端后必须先运行只读状态核验，确认发布标记、三个构建入口哈希、三服务状态和本机HTTP，再决定是否需要安全续跑或回滚。

恢复终端后，全sudo只读终态核验确认发布标记为`800fd8dc`；远端`dist/index.js`、`dist/aiWorker.js`、`dist/scheduler.js`的SHA-256均与本地补丁构建完全一致；Web、Worker、Scheduler均为`active`，本机HTTP为200。中断发生于Workbench会话而非应用发布。发布标记的首次回读因普通用户无读取权限而返回`Permission denied`，但写入使用`sudo tee`；随后使用sudo只读核验成功确认标记和健康状态。未再次执行发布、未修改密钥、未执行DDL，也未发起Provider、ERP、Heartbeat或资格Run。
