# 青岛独立站代码与数据同步验收记录（2026-08-31）

## 已完成的受控步骤

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 同步前备份 | 通过 | 青岛ECS已创建并校验代码归档与MySQL逻辑备份；备份保留在服务器受限目录。 |
| 源库导入 | 通过 | 托管源库273张业务表已导入隔离暂存库，随后以源端优先、`INSERT IGNORE`方式合并独立站历史记录。 |
| 数据库切换 | 通过 | 生产库已切换到合并后的273张表版本，旧生产库保留为独立回滚库；唯一外键已恢复。 |
| 代码与迁移 | 通过 | 最新代码已构建并同步；0187至0189迁移账本为`succeeded`。 |
| 服务与HTTP | 通过 | Web、Worker、Scheduler、Nginx与MySQL均为active；本机和公网HTTPS请求均返回200。 |

## 待修复的页面验收异常

浏览器访问`https://kuahaixing.com`时，页面标题可读取为“AMZ 全链路”，但渲染视图显示白屏。该现象发生在数据、代码同步完成后的浏览器级Smoke Test中，尽管服务器本机及公网HTTP状态均为200。必须在交付前通过浏览器控制台、静态资源和服务日志定位并修复，不能仅以HTTP 200视为前端可用。

后续浏览器检查确认主入口模块脚本已成功加载，`#root`包含完整登录页DOM，根容器及首层布局均为可见状态、透明度为1，且控制台没有错误输出。因此，初次截图白屏尚未复现为可定位的应用异常；需要继续以浏览器刷新和交互方式完成最终视觉验收，避免基于单一截图改动主题或布局。

最终刷新验收已显示完整登录页、说明文案与“登录使用”按钮，未复现白屏。发布过程中曾因同步时排除pnpm隐藏存储目录导致生产端缺少`archiver`依赖；已通过锁文件重新安装依赖后恢复，服务日志确认应用重新监听本机3000端口。本机HTTP和公网HTTPS均返回200。

## 回滚点

| 回滚资源 | 状态 | 说明 |
| --- | --- | --- |
| 青岛代码归档 | 已校验 | 同步前代码归档保存在ECS受限备份目录，包含校验和。 |
| 青岛MySQL逻辑备份 | 已校验 | 同步前生产库逻辑备份已完成压缩完整性检查。 |
| 旧生产数据库 | 已保留 | 切换前的生产库保留为独立回滚库；本轮未删除。 |
| 当前生产数据库 | 已验证 | 使用托管源库最新数据并以`INSERT IGNORE`保留目标环境不冲突的历史记录。 |

## www子域名404修复（2026-08-31）

用户直接访问`http://www.kuahaixing.com`时收到Nginx 404。根因是原80端口虚拟主机只对根域名设置条件跳转，`www`未命中该条件而落入Certbot生成的`return 404`兜底。DNS解析无差异，两者均指向青岛ECS。

已创建Nginx站点配置备份，并将80端口的根域名与www统一设置为301跳转；随后以Certbot将现有证书扩展为同时覆盖`kuahaixing.com`和`www.kuahaixing.com`。Nginx语法测试、无中断重载、服务器端四条URL响应、证书SAN和浏览器端直接输入www均已验证。

| 访问地址 | 最终结果 |
| --- | --- |
| `http://kuahaixing.com` | 301 → `https://kuahaixing.com` |
| `https://kuahaixing.com` | 200，应用登录页 |
| `http://www.kuahaixing.com` | 301 → `https://www.kuahaixing.com` |
| `https://www.kuahaixing.com` | 200，应用登录页；证书含两个域名 |

## 安全边界

本记录不包含环境配置、数据库连接、对象存储凭据、模型密钥、下载URL或业务正文。首次知识蒸馏真实试点仍未执行。
