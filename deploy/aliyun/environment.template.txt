# Copy this file to deploy/aliyun/.env only on the ECS host.
# Never commit the generated .env file or replace these placeholders with production values in Git.

NODE_ENV=production
PORT=3000
# MySQL runs on the private Docker network and is never published to the ECS network.
# URL-encode MYSQL_APP_PASSWORD in DATABASE_URL if it contains @, :, /, ?, # or %.
MYSQL_DATABASE=amz_fullchain
MYSQL_APP_USER=amz_app
MYSQL_APP_PASSWORD=CHANGE_ME_APP_PASSWORD
MYSQL_ROOT_PASSWORD=CHANGE_ME_ROOT_PASSWORD
DATABASE_URL=mysql://amz_app:CHANGE_ME_URL_ENCODED_APP_PASSWORD@mysql:3306/amz_fullchain
JWT_SECRET=GENERATE_A_LONG_RANDOM_SECRET
# Generate with: openssl rand -base64 48. Required by production startup validation.
TOOL_SECRET_KEY=GENERATE_A_SEPARATE_LONG_TOOL_SECRET
# Shared secret for external cron invocations; keep blank only if no scheduled HTTP trigger is used.
SCHEDULED_TASK_SECRET=GENERATE_A_SCHEDULED_TASK_SECRET

# Independent local authentication: email/password + JWT, no Manus OAuth UI.
AUTH_MODE=local
VITE_AUTH_MODE=local

# Aliyun OSS private bucket via its S3-compatible endpoints.
# Server-side upload/download calls use the Qingdao same-region internal endpoint.
# S3_PUBLIC_ENDPOINT is used only to sign browser-facing upload/download URLs.
STORAGE_PROVIDER=oss
S3_ENDPOINT=https://oss-cn-qingdao-internal.aliyuncs.com
S3_PUBLIC_ENDPOINT=https://oss-cn-qingdao.aliyuncs.com
S3_REGION=cn-qingdao
S3_BUCKET=amz-fullchain-private
S3_ACCESS_KEY_ID=REPLACE_WITH_RAM_ACCESS_KEY_ID
S3_SECRET_ACCESS_KEY=REPLACE_WITH_RAM_ACCESS_KEY_SECRET
S3_FORCE_PATH_STYLE=false
STORAGE_PRESIGN_EXPIRES_SECONDS=900

# Encrypted MySQL logical backup to the same private OSS Bucket.
# Generate with: openssl rand -base64 48
BACKUP_ENCRYPTION_KEY=GENERATE_A_SEPARATE_LONG_BACKUP_SECRET
BACKUP_OSS_PREFIX=backups/mysql
BACKUP_RETENTION_DAYS=14

# Existing OpenAI-compatible model gateway. Do not place these values in source control.
LLM_PROVIDER=external
EXTERNAL_LLM_BASE_URL=https://YOUR_LLM_GATEWAY/v1
EXTERNAL_LLM_API_KEY=REPLACE_WITH_MODEL_GATEWAY_KEY
EXTERNAL_LLM_MODEL=YOUR_DEFAULT_MODEL

# AI job worker. Keep web, worker and scheduler on the same RDS instance.
AI_JOB_IN_PROCESS=false
AI_JOB_RUNNER_MODE=worker
AI_JOB_WORKER_CONCURRENCY=2
AI_JOB_WORKER_HEARTBEAT_MS=15000
AI_JOB_WORKER_POLL_MS=3000
AI_JOB_WORKER_SHUTDOWN_GRACE_MS=30000
AI_JOB_WORKER_STALE_MS=300000

# These must remain unset in independent mode.
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
VITE_FRONTEND_FORGE_API_KEY=
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=
OAUTH_SERVER_URL=
